# Where replies from !free should go.
# reply_target supports "channel", "notice", or a fixed "#channel".
reply_target: "channel"

# Treat disk status older than this as stale/down.
stale_after_seconds: 600
